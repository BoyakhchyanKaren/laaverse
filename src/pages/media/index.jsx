import { MediaBanner, MediaList, ApplyBanner } from "components/media";

const Media = () => {
    return (
        <div>
            <MediaBanner />
            <MediaList />
            <ApplyBanner />
        </div>
    )
}

export default Media;